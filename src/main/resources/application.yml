spring:
  ai:
    openai:
      # 请替换为您的DeepSeek API密钥
      # 可以通过设置环境变量 DEEPSEEK_API_KEY 或直接替换下面的值
      api-key: ${DEEPSEEK_API_KEY:your-deepseek-api-key}
      base-url: https://api.deepseek.com
      chat:
        options:
          model: deepseek-chat
          temperature: 0.7
